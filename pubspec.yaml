name: bhagvat_geeta_app
description: "Bhagavad Geeta, Panchang, and Rashi Bhavishya Flutter App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  dio: ^5.7.0
  flutter_tts: ^4.2.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.3.5
  geolocator: ^13.0.2
  flutter_dotenv: ^5.2.1
  google_fonts: ^6.2.1
  connectivity_plus: ^6.1.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
